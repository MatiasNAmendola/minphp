<?php
	function usuarios($datos = false){
		// Creamos la conexion mysql y validamos que todo sea correcto para proceder.
		//$db = new mysqli('localhost', 'root', '1234', 'kiin.local');
		//if($db->connect_errno > 0){
		//    die('No se puede contectar a la base de datos especificada [' . $db->connect_error . ']');
		//}

		//$resultado = array(); // En esta variables metes el resultado del query o querys.
		// Si todo salio bien pasamos al los querys! y a definir las variables.
		//---------------------------------------------------
		

		//$resultado[] = 'Hola';


		//---------------------------------------------------
		//---------------------------------------------------
		//$db->close();
		// Finalmente retornamos el array con los datos dentro
		//return $resultado;
	}
?>
